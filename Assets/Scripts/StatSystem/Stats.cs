using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * This class provides the basic structure for the stats system;
 * the individual stats per enemy type and characters will be in "case" statements
 * abfrage zuerst ob enemy-attribut o. char > per namen-attribut festlegen welche stats
 * stat script übergibt dem fight script die stats
*/

public class Stats : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
